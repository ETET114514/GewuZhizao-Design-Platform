# Interior model files

Place glTF 2.0 `.glb` assets in this directory and register their relative paths in `../catalog.json`.

Keep related models in subdirectories such as `living/`, `bedroom/`, `kitchen/`, `bathroom/`, `lighting/`, and `decor/`.
